import { useState } from "react"

export function TodoListItem({children, isComplete}){
   
    return(
        <div>
            <label htmlFor="checkb">input onClick={changeCheckState} value={showCheckState} id="checkb" type="checkbox"></input>
            </label>
           
        </div>
        
    ) 
}